apiVersion: v1
kind: Service
metadata:
  name: {{ include "ratedpower.fullname" . }}
  labels:
    {{- include "ratedpower.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "ratedpower.selectorLabels" . | nindent 4 }}
